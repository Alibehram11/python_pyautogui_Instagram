Before running the program, google must be open in the background and 
the program you are running (visual studio, pychar, etc.) must be full screen.

We download our libraries

pip install pyautogui

pip install time

pip install webbrowser
